import pandas as pd
import pandas_datareader.data as web
import datetime as dt
import matplotlib.pyplot as plt

#define the start and end datas for the stock data
start = dt.datetime(2015,1,1)
#end = dt.datetime(2020,2,29)
end = dt.datetime.now()


#developing in test file - updated soon
# Defining Variables for the Equations
#
# a1 = "AAPL"
# a1data = web.DataReader(a1, 'yahoo', start, end)
# a2data = web.DataReader(a2, 'yahoo', start, end)
# a3data = web.DataReader(a3, 'yahoo', start, end)
# a4data = web.DataReader(a4, 'yahoo', start, end)
# a5data = web.DataReader(a5, 'yahoo', start, end)
# a6data = web.DataReader(a6, 'yahoo', start, end)
# a7data = web.DataReader(a7, 'yahoo', start, end)
# a8data = web.DataReader(a8, 'yahoo', start, end)
# a9data = web.DataReader(a9, 'yahoo', start, end)
# a10data = web.DataReader(a10, 'yahoo', start, end)
# a11data = web.DataReader(a11, 'yahoo', start, end)
# a12data = web.DataReader(a12, 'yahoo', start, end)
# a13data = web.DataReader(a13, 'yahoo', start, end)
# a14data = web.DataReader(a14, 'yahoo', start, end)
# a15data = web.DataReader(a15, 'yahoo', start, end)
# a16data = web.DataReader(a16, 'yahoo', start, end)
# a17data = web.DataReader(a17, 'yahoo', start, end)
# a18data = web.DataReader(a18, 'yahoo', start, end)
# a19data = web.DataReader(a19, 'yahoo', start, end)
# a20data = web.DataReader(a20, 'yahoo', start, end)





#print(a1data)